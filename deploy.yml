---
name: SRE Deployment Pipeline
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to k3s
        run: |
          sudo k3s kubectl apply -f nginx-app.yaml
